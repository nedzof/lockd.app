const { createClient } = require('@supabase/supabase-js'); require('dotenv').config(); const supabaseUrl = process.env.VITE_SUPABASE_URL; const supabaseKey = process.env.VITE_SUPABASE_ANON_KEY; const supabase = createClient(supabaseUrl, supabaseKey); async function checkTransaction() { const txid = 'aba0284aae55c76270f4b2f80aac694f7afdcf4b5ef1148c8147cba6321fc187'; const { data: post, error: postError } = await supabase.from('Post').select('*').eq('id', txid).single(); if (postError) { console.error('Error querying post:', postError); return; } console.log('Post data:', post); const { data: lock, error: lockError } = await supabase.from('LockLike').select('*').eq('txid', txid).single(); if (lockError) { console.error('Error querying lock:', lockError); return; } console.log('Lock data:', lock); } checkTransaction().catch(console.error);
