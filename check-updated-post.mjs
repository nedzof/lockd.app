import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabaseUrl = process.env.VITE_SUPABASE_URL; const supabaseKey = process.env.VITE_SUPABASE_ANON_KEY; const supabase = createClient(supabaseUrl, supabaseKey); async function checkPost() { const txid = 'aba0284aae55c76270f4b2f80aac694f7afdcf4b5ef1148c8147cba6321fc187'; const { data: post, error } = await supabase.from('Post').select('*').eq('id', txid).single(); if (error) { console.error('Error:', error); return; } console.log('Post data:', post); } checkPost().catch(console.error);
