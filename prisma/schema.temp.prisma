generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = "postgres://postgres:4H2fquLGlQHFjrfdeM8Qyxlcmsba%2FS53VirCxqlLOILWLlI9KRYrKUqqrPxC5DIHhkO3HPwxtFrIrVWY410RPg%3D%3D@db.aqqxfcazqwjuyjjmuuxn.supabase.co:6543/postgres?pgbouncer=true&schema=public&sslmode=require"
}

model Post {
  id            String   @id @default(cuid())
  txid          String   @unique
  content       String
  author_address String
  media_type    String?
  block_height  Int
  amount        Int?
  unlock_height Int?
  description   String?
  created_at    DateTime @default(now())
  tags          String[]
  metadata      Json?
  is_locked     Boolean  @default(false)
  lock_duration Int?

  @@index([author_address])
  @@index([created_at])
  @@index([block_height])
} 